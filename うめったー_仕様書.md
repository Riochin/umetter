# うめったー アプリケーション仕様書

| 項目 | 内容 |
|---|---|
| アプリ名 | うめったー |
| ドキュメント名 | うめったー 機能・画面仕様書 |
| バージョン | v2.2 |
| 最終更新日 | 2026-05-29 |
| ステータス | スプリント2（実装フェーズ）進行中 |
| 対象プラットフォーム | iOS（Swift / SwiftUI） |
| 想定ユーザー | 津田塾大学の学生および教職員 |

> **本書の位置づけ**
> 津田塾生専用の時間割管理・友達時間割共有と、完全匿名SNSを統合したモバイルアプリ「**うめったー**」の機能・画面仕様を定義する。
> 第5章「データモデル」「第6章 API仕様」は要件から導出した**実装提案**。
> **v2.2 改訂**：旧「TsudaNet」表記を全廃し「うめったー」に統一。ボトムナビを4項目（ホーム/通知/カレンダー/マイページ）へ再編し、検索・チャットをホーム配下に統合。

---

## 1. アプリケーション概要

### 1.1 目的・ターゲット

- **ターゲット**：津田塾大学の学生、および教職員。
- **目的**
  - 既存ポータルの課題（セッション切れ・動作の重さ・古いUI）を解消し、時間割管理と友達同士での時間割共有をスムーズにする。
  - 完全匿名（一部公認）のSNSを通じて、学内の疑問解消・サークル勧誘・雑談・教員アナウンスを安心・安全に行えるようにする。

### 1.2 アプリ全体の基本ルール

| ルール | 内容 |
|---|---|
| 認証制限 | 大学付与メールアドレス（`@gm.tsuda.ac.jp`）保有者のみ登録可能。 |
| 友達関係 | 一方通行のフォローは採用せず、**相互承認した友達**のみを繋ぐ。 |
| 匿名性の徹底 | 匿名投稿は、友達であっても投稿主を特定できない（完全匿名）。 |

### 1.3 開発体制（3班）

| 班 | 技術領域 | メンバー |
|---|---|---|
| バックエンド | Go | あんなさん、りおちゃん |
| フロントエンド | Swift | あきぽん、はーさん、あかり |
| UI/UX設計 | Figma | あいちゃん |

---

## 2. デザインガイドライン（モックアップ準拠）

### 2.1 カラーパレット
- **プライマリ：えんじ色（深い臙脂／マルーン）** … ボタン・ロゴ・梅いいね（塗り）・休講バナー・アクセント。
- **背景：クリーム／アイボリー系** … タイムラインや各画面の地色。
- **ニュートラル：グレージュ** … プレースホルダー、未読アバター、時間割空きコマ。

> ※ HEXは実装時にFigmaの確定値を使用すること。

### 2.2 トーン
- 津田塾をリスペクトしたモダンで落ち着いた印象。梅モチーフ（梅いいね・うめこ等）を随所に用いる。

---

## 3. ナビゲーション構成

**ボトムナビゲーション（4項目）**

| タブ | 役割 | 対応画面 |
|---|---|---|
| ホーム | 匿名タイムライン＋検索＋チャット入口＋カテゴリ | 画面2（タイムライン）／投稿作成／投稿詳細・モデレーション／検索／分類・リスト／チャット |
| 通知 | 通知履歴 | 画面7（通知履歴） |
| カレンダー | 時間割・友達の時間割共有 | 画面4（時間割）／画面5（時間割共有） |
| マイページ | アカウント・公開範囲設定 | 画面8（マイページ） |

**トップヘッダー**：うめったーロゴ＋検索アイコン＋通知（ベル）アイコン。

- **検索**：独立タブは設けず、ホームの検索アイコン／検索バーから投稿・授業・友達・カテゴリを横断検索。
- **チャット**：独立タブは設けず、ホームからオープンチャット一覧へ遷移。
- **管理者ビュー**：`role = admin` のみアクセス可能な別画面（ナビ非表示）。

---

## 4. 画面遷移図

```
[起動]
  │
  ├──► [1. ログイン/新規登録]
  │      │ (登録時：メアドから学年・学科を自動抽出)
  │      ▼
  ├──► [2. ホーム：タイムライン（うめったー）]   ◀ ボトムナビ
  │      ├── [2.1 投稿作成]（右下＋ボタン）
  │      ├── [2.2 投稿詳細・モデレーション]（…→ 友達申請/通報/ブロック）
  │      ├── [2.3 検索（投稿/授業/友達/カテゴリ）]
  │      ├── [2.4 分類・リスト（フィード/リスト・カテゴリ）]
  │      └── [2.5 オープンチャット一覧/詳細]
  │
  ├──► [3. 通知：通知履歴]                       ◀ ボトムナビ
  │
  ├──► [4. カレンダー：時間割]                   ◀ ボトムナビ
  │      ├── [4.1 授業詳細・出欠メモ]
  │      ├── [4.2 授業検索・追加]
  │      └── [4.3 時間割共有（自分 / 友達）]
  │
  ├──► [5. マイページ：公開範囲設定]             ◀ ボトムナビ
  │
  └──► [Admin. 管理者ビュー（admin限定）]
```

---

## 5. 画面詳細・機能仕様

### 5.1 【画面1】ログイン・新規登録

**UI（モックアップ）**：うめったーロゴ／「ユーザー名とパスワードを入力してください」／English切替／ユーザー名・パスワード入力フィールド／えんじ色「ログイン」ボタン。

**機能**
- 新規登録は `@gm.tsuda.ac.jp` 必須。
- メアドから**学科コード・入学年度を自動抽出**してDB保存（例：`ab24123@gm.tsuda.ac.jp` → 学科 `ab`／2024年入学＝現3年）。手動詐称は不可（サーバー側抽出）。

### 5.2 【画面2】ホーム：タイムライン（うめったー）

**UI（モックアップ）**
- 上部にコンポーザ「**What's happening? / いまどうしてる？**」＋編集ペンアイコン。
- ヘッダーに**検索アイコン・通知ベル**。
- 投稿カードに各種アクションアイコン（コメント・**梅いいね**・共有）。投稿カードは3種：**通常 / タグ付き / 画像付き**。
- **右下にフローティング「＋」ボタン**（新規投稿）。
- ホームから**チャット**・**分類/リスト**・**検索**へ遷移。

**機能**
| 機能 | 仕様 |
|---|---|
| 完全匿名 | 一般投稿（`post_type = 0`）はユーザー名・アバター・IDをフロントに返却しない。 |
| 梅いいね！ | 「UmeLikes: 123」表示。1投稿1ユーザー1回。線→塗り（点灯）に切替、カウント即時反映。 |
| モデレーション | 各投稿の「…」→ アクションシートで **友達申請 / 通報 / ブロック**。 |
| 添付 | 画像 または PDF を1点まで。 |

**5.2.1 投稿作成（投稿入力）**
- テキスト最大280文字、プレースホルダ「いまどうしてる？(完全匿名)」。
- 添付（📎）・カメラ（📷）・ハッシュタグ（#）。タグ分類に対応。

**5.2.2 投稿詳細・モデレーション**
- 上部に休講情報バナー「**[重要] 休講情報: SitesGoogleLink**」を表示。
- 「…」メニューから 友達申請 / 通報 / ブロック を実行。
- ※匿名性を保ちつつ、内部DBでは投稿者IDを保持し、通報時のみ管理者が確認可能。

**5.2.3 検索**
- ホームの検索アイコン／検索バーから、投稿・授業・友達・カテゴリを横断検索。

**5.2.4 分類・リスト**
- 上部タブ「**フィード / リスト**」。カテゴリでタイムラインをフィルタ。
  - 学芸学部 / 総合政策学部 / 2026年度生 / サークル勧誘 / 教授のつぶやき

**5.2.5 オープンチャット**
- ホームからルーム一覧へ。検索バー（チャットルーム）＋フィルタ。各ルームに**参加人数・最新メッセージ・未読バッジ**を表示。
- ルーム内はタイムライン型チャット。入室時に**ランダムなニックネーム（例：うめこ1号）**を自動割り当て、匿名性を維持。

### 5.3 【画面3】通知：通知履歴

**機能**
- 授業前通知（例：1限開始20分前「次の授業は〇〇（教室: 7201）です」）。
- 教務通知（履修登録／放棄期間のリマインド）。
- 通知センターに整然と表示し、アプリ内一覧でも後から確認可能。

### 5.4 【画面4】カレンダー：時間割

**UI（モックアップ）**：月〜金 × 1〜5限グリッド（土曜・6限は設定でON/OFF）。フラットな「時間割コマ」カードUI。

**機能**
- 空きコマタップ → 授業検索画面。`classes`（シラバス由来）を**授業名・教員名で検索**しワンタップ登録。
- 登録授業タップ → 詳細ポップアップで「出席・欠席・遅刻」カウント＋自由メモ（`user_timetables.memo`）。
- 大学データ連携で**休講を赤字ラベル自動表示**。

**5.4.1 時間割共有**
- タブ「**自分の時間割表 / 友達の時間割表**」を切り替え。
- 友達申請は相互承認制。Bが承認した時点で互いの時間割が閲覧可能（`status = 'approved'`）。
- 友達の時間割は公開範囲をクリアしたものを**読み取り専用**で表示。

### 5.5 【画面5】マイページ：公開範囲設定（UC3）

**UI**：アカウント情報（学部・入学年度・表示名）＋公開範囲のセグメンテッドコントロール。

**機能（神機能：縦の繋がり自動承認）**

| 公開範囲 | 公開対象 | 判定ロジック |
|---|---|---|
| 非公開 | 誰にも見せない | — |
| 友達限定 | 相互承認した友達のみ | `friendships.status = 'approved'` |
| 同じ学部の後輩【一押し】 | 同学科かつ自分より後の入学者 | `department` 一致 かつ 相手の `admission_year` > 自分の `admission_year`。**友達申請なしで閲覧許可**。 |
| 全員 | ログイン中の全津田塾生 | — |

### 5.6 【Admin】管理者ビュー（admin限定）

**UI（モックアップ）**：左サイドバー（ダッシュボード／通報件数／投稿数／通報）。
- **サマリーカード**：通報件数（例 0）／新規ユーザー（例 11）／投稿数（例 26）／累計いいね数（例 223）。
- **通報一覧**（棒グラフ）／**投稿一覧**（棒グラフ）／**ユーザー一覧**（学部・登録数・いいね数の表）。

**機能**：`role = admin` のみアクセス可。通報の確認・対応、各種指標のモニタリング。ボトムナビには表示しない。

---

## 6. デザインシステム（コンポーネント一覧 / Figma準拠）

| 区分 | コンポーネント | 備考 |
|---|---|---|
| ナビ | トップヘッダー | うめったーロゴ＋検索＋通知ベル |
| ナビ | ボトムナビゲーション | ホーム/通知/カレンダー/マイページ |
| タイムライン | 投稿カード（通常） | タイトカード |
| タイムライン | 投稿カード（タグ付き） | タグチップ表示 |
| タイムライン | 投稿カード（画像付き） | 画像領域あり |
| タイムライン | 梅いいねボタン（線） | 未押下状態・カウント表示 |
| タイムライン | 梅いいねボタン（塗り） | 押下状態（えんじ色点灯） |
| ボタン | えんじ色ボタン | プライマリ（ログイン / Post） |
| ボタン | 白色ボタン | セカンダリ |
| ボタン | テキストボタン | English 等 |
| 入力 | 入力フィールド（通常） | |
| 入力 | 入力フィールド（アイコン付き） | 先頭アイコン |
| 入力 | テキストエリア | 投稿本文（完全匿名プレースホルダ） |
| カード | 休講情報バナー | 【休講情報】Kyuko info |
| カード | チャットルーム項目 | アイコン＋人数＋最新メッセージ＋未読バッジ |
| カード | 時間割コマ | サンプルセル |
| メニュー | アクションシート（投稿メニュー） | ブロック / 報告 / フレンドリクエスト |
| アイコン | ナビアイコン | ホーム / 検索 / 通知 / カレンダー / チャット |
| アイコン | 操作アイコン | プラス / その他(…) / ブロック / 通報 / etc. |

> ※ アイコンセットには検索・チャットも含むが、ボトムナビは4項目（ホーム/通知/カレンダー/マイページ）。検索・チャットはホーム配下で使用する。

---

## 7. データモデル（提案）

> RDB（PostgreSQL / Cloudflare D1 等）想定。

### 7.1 エンティティ概要
```
users 1 ──< user_timetables >── 1 classes
users 1 ──< friendships >── 1 users   (requester / addressee)
users 1 ──< posts                     (内部のみ保持・匿名表示)
posts 1 ──< ume_likes >── 1 users     (UNIQUE: post_id × user_id)
posts 1 ──< reports                   (管理者のみ参照)
users 1 ──< blocks
chat_rooms 1 ──< chat_messages
users 1 ──< notifications
```

### 7.2 主要テーブル

**users**：id / email / password_hash(bcrypt) / department / admission_year / display_name / role(`student`/`faculty`/`admin`) / timetable_visibility(`private`/`friends`/`juniors`/`all`) / created_at

**classes**：id / name / teacher_name / day_of_week / period / room / is_canceled（休講）

**user_timetables**：id / user_id / class_id / memo / count_present / count_absent / count_late

**friendships**：id / requester_id / addressee_id / status(`pending`/`approved`/`rejected`)

**posts**：id / author_id（内部のみ・匿名時は返却しない）/ post_type（`0`=匿名）/ category(`all`/`department`/`circle`/`faculty`/`year`) / body(≤280) / attachment_url / is_pinned / created_at

**ume_likes**：post_id / user_id（`(post_id,user_id)` UNIQUE）

**reports / blocks / chat_rooms / chat_messages / notifications**：概念図に準拠。

---

## 8. API仕様（提案）

### 8.1 共通
- ベースURL：`/api/v1`、JSON。
- 認証：保護APIは `Authorization: Bearer <JWT_TOKEN>` 必須。

### 8.2 主要エンドポイント（抜粋）

| 分類 | メソッド / パス | 概要 |
|---|---|---|
| 認証 | `POST /auth/register` | 登録（メアドから学科・入学年抽出） |
| 認証 | `POST /auth/login` | ログイン、JWT発行 |
| タイムライン | `GET /posts?category=all` | 取得（匿名投稿は投稿者情報マスク） |
| タイムライン | `POST /posts` | 投稿（テキスト＋添付1点） |
| タイムライン | `POST /posts/{id}/like` | 梅いいね（重複不可） |
| 検索 | `GET /search?q=&type=` | 投稿/授業/友達/カテゴリ横断検索 |
| モデレーション | `POST /posts/{id}/report` | 通報 |
| モデレーション | `POST /posts/{id}/block` | 非表示 |
| 時間割 | `GET /classes?keyword=` | 授業検索 |
| 時間割 | `POST /timetables` | 登録 |
| 時間割 | `PATCH /timetables/{id}` | 出欠・メモ更新 |
| 友達 | `POST /friends/request` | 友達申請 |
| 友達 | `PATCH /friends/{id}` | 承認/拒否 |
| 友達 | `GET /friends/{userId}/timetable` | 友達時間割閲覧（公開範囲チェック） |
| チャット | `GET /chat/rooms` | ルーム一覧 |
| 通知 | `GET /notifications` | 通知履歴 |
| 設定 | `PATCH /me/visibility` | 公開範囲変更 |
| 管理 | `GET /admin/dashboard` | 指標（通報件数/新規/投稿/いいね） |
| 管理 | `GET /admin/reports` | 通報一覧（admin） |

> **匿名マスク**：`GET /posts` の `post_type = 0` は `author_id` 等の識別情報を除外して返却。

---

## 9. 認証・セキュリティ仕様

### 9.1 パスワード
- 平文保存禁止。Go側で **bcrypt** ハッシュ化（将来 **Argon2id**）。

### 9.2 JWT
1. ログイン成功時にサーバーが署名付きJWTを発行・返却。
2. Swift側は保護APIに `Authorization: Bearer <JWT_TOKEN>` を付与。
3. サーバーは毎リクエストで署名・有効期限・`role` を検証。クライアント判定に依存しない。

---

## 10. プッシュ通知・ウィジェット

### 10.1 プッシュ通知
- 授業前通知（教室名込み）、履修登録/放棄のリマインド、通知履歴の一覧化。

### 10.2 iOSウィジェット（WidgetKit）
- ホーム画面に「今日これからの授業＋教室」をコンパクト表示。

---

## 11. 開発スプリントロードマップ（5/22 〜 6/19）

```
 5/22(金)        5/29          6/5           6/12        6/19
   ├─スプリント1─┤─スプリント2─┤─スプリント3─┤─スプリント4─┤(成果発表)
   ▼             ▼             ▼             ▼           ▼
 [起動]        [認証/API疎通]  [機能統合]    [テスト調整]  [リリース]
```

- **S1（5/22-5/28）完了 🎉**：設計確定・環境構築（ER図/ユースケース図、Goローカル、Figmaモック）。
- **S2（5/29-6/4）進行中 🔥**：bcrypt登録・JWTミドルウェア、DBスキーマ、デプロイ疎通／ログイン・登録画面、APIクライアント。
- **S3（6/5-6/11）**：タイムラインAPI（匿名マスク）・授業検索/登録API／時間割UI・タイムラインUI・投稿作成UI。
- **S4（6/12-6/18）**：友達申請/承認API・後輩公開認可・管理者通報API／友達時間割画面・最終デバッグ。
- **成果発表（6/19）**：デモ＋スライド。

---

## 付録：v2.2 改訂点
- 「TsudaNet」表記を全廃し「うめったー」に統一（ロゴ・ヘッダー・ログイン含む）。
- ボトムナビを4項目（ホーム/通知/カレンダー/マイページ）に再編。
- 検索・チャットはタブを廃止し、**ホーム配下**からアクセスする構成に変更。
- 時間割共有をカレンダー（画面4）配下に整理。
